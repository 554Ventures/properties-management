// Product branding shared by both apps. The assistant's user-facing name —
// UI labels, a11y announcements, error copy, and the API's system prompt all
// read this one constant, so a rename is a one-line change. Legal documents
// (terms/privacy) deliberately spell the name out: legal copy changes only
// through review, never implicitly via a rename here.
export const ASSISTANT_NAME = 'Roost';
